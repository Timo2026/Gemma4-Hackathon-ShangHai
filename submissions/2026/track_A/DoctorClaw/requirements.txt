# DoctorClaw 主 Python 依赖入口
# 完整依赖列表见 backend/requirements.txt
# Agent Harness 额外依赖见 backend/requirements-agent.txt（可选）
#
# 安装方式：
#   项目根目录：pip install -r requirements.txt
#   或 cd backend && pip install -r requirements.txt

-r backend/requirements.txt
